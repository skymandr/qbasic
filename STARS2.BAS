DECLARE SUB CENTER (ROW!, TEXT$)
DECLARE SUB SHIPINIT ()
DECLARE SUB STARS ()
DECLARE SUB INITSTARS ()
DIM SHARED FX(1 TO 9)
DIM SHARED FY(1 TO 9)
DIM SHARED BX(1 TO 9)
DIM SHARED BY(1 TO 9)
DIM SHARED BBX(1 TO 9)
DIM SHARED BBY(1 TO 9)
DIM SHARED SHIP(1 TO 675)
DIM SHARED SY
DIM SHARED HYP
DIM SHARED HYPA
C = 17
V = 1

SCREEN 13
CLS

INITSTARS

SY = 100
DO
STARS
IF HYP = 1 THEN HYPA = 1
IF TEXT = 1 THEN TEXT$ = "DEMON SOFTWARE PROUDLY PRESENTS"
IF TEXT = 2 THEN TEXT$ = ""
IF TEXT = 3 THEN TEXT$ = "THE ONLY SPACE DEMO ON THIS SCHOOL"
IF TEXT = 4 THEN TEXT$ = ""
IF TEXT = 5 THEN TEXT$ = "- THE HYPERSPACE JUMP -"
IF TEXT = 6 THEN TEXT$ = ""
IF TEXT = 8 THEN TEXT$ = "PREPARE TO JUMP INTO HYPERSPACE..."
IF TEXT = 9 THEN TEXT$ = ""
IF TEXT = 10 THEN TEXT$ = "3"
IF TEXT = 11 THEN TEXT$ = "2"
IF TEXT = 12 THEN TEXT$ = "1"
IF TEXT = 13 THEN TEXT$ = "HYYYPEERSPAACE !"
IF TEXT = 14 THEN HYP = 1

IF HYP = 0 THEN
 C = C + V
 IF C > 30 THEN V = -1
 IF C < 17 THEN V = 1: TEXT = TEXT + 1
 COLOR INT(C)
 LOCATE 12, 21 - LEN(TEXT$) / 2
 PRINT TEXT$
END IF

IF HYP = 1 THEN HYPTIME = HYPTIME + 1
HYP = 1
LOOP UNTIL INKEY$ = CHR$(27)

SUB CENTER (ROW, TEXT$)

LOCATE ROW, 21 - LEN(TEXT$) / 2
PRINT TEXT$;

END SUB

SUB HYPERSPACE

FOR I = 1 TO 9
LINE (0, FY(I))-(FX(I), FY(I)), 15
LINE (0, BY(I))-(BX(I), BY(I)), 7
LINE (0, BBY(I))-(BBX(I), BBY(I)), 8

FX(I) = FX(I) + 10
BX(I) = BX(I) + 5
BBX(I) = BBX(I) + 2

RANDOMIZE TIMER
LINE (319, 0)-(319, 200), 0
IF FX(I) > 330 THEN FX(I) = 0: FY(I) = INT(RND * 200)
IF BX(I) > 330 THEN BX(I) = 0: BY(I) = INT(RND * 200)
IF BBX(I) > 330 THEN BBX(I) = 0: BBY(I) = INT(RND * 200)
NEXT I


END SUB

SUB INITSTARS

FOR I = 1 TO 9
RANDOMIZE TIMER
FX(I) = INT(RND * 320)
FY(I) = INT(RND * 200)
BX(I) = INT(RND * 320)
BY(I) = INT(RND * 200)
BBX(I) = INT(RND * 320)
BBY(I) = INT(RND * 200)

NEXT I


END SUB

SUB STARS

FOR I = 1 TO 9

PSET (FX(I), FY(I)), 15
PSET (BX(I), BY(I)), 7
PSET (BBX(I), BBY(I)), 8


'IF HYPA = 0 THEN
 PSET (FX(I) - 4, FY(I)), 0
 PSET (BX(I) - 3, BY(I)), 0
 PSET (BBX(I) - 2, BBY(I)), 0
'
'END IF

FX(I) = FX(I) + 4
BX(I) = BX(I) + 3
BBX(I) = BBX(I) + 2

IF HYP = 1 THEN FX(I) = FX(I) - 3


RANDOMIZE TIMER
LINE (319, 0)-(319, 200), 0
IF FX(I) > 330 THEN FX(I) = 0: FY(I) = INT(RND * 200)
IF BX(I) > 330 THEN BX(I) = 0: BY(I) = INT(RND * 200)
IF BBX(I) > 330 THEN BBX(I) = 0: BBY(I) = INT(RND * 200)
NEXT I

END SUB

