DECLARE SUB DRWSCR ()
DECLARE SUB CENTER (ROW!, TEXT$)
SCREEN 12
DIM SHARED ALT(1 TO 8000)
CLS


DRWSCR
RANDOMIZE TIMER

TOP:
DRWSCR
CENTER 3, "- JAG VISAR -"
TIMES = TIMES + 1
FOR I = 1 TO TIMES
ALT(TIMES) = INT(RND * 3) + 1
IF ALT(I) = 1 THEN GOSUB ETT
IF ALT(I) = 2 THEN GOSUB TVA
IF ALT(I) = 3 THEN GOSUB TRE
FOR P = 1 TO 2000: NEXT P
DRWSCR
FOR P = 1 TO 1000: NEXT P
NEXT I


DRWSCR
CENTER 3, "             "

FOR I = 1 TO TIMES
CENTER 3, "- DIN TUR -"
DO
A$ = INKEY$
LOOP UNTIL A$ = "1" OR A$ = "2" OR A$ = "3" OR A$ = CHR$(27)
IF A$ = "1" AND ALT(I) = 1 THEN RIGHT = 1: GOSUB ETT
IF A$ = "2" AND ALT(I) = 2 THEN RIGHT = 1: GOSUB TVA
IF A$ = "3" AND ALT(I) = 3 THEN RIGHT = 1: GOSUB TRE

IF RIGHT = 0 THEN GOTO DIE

FOR P = 1 TO 500: NEXT P

DRWSCR
RIGHT = 0
NEXT I

FOR P = 1 TO 5000: NEXT P
KLAR = KLAR + 1
GOTO TOP

ETT:
LINE (101, 101)-(199, 199), 4, BF
SOUND 100, 2
RETURN

TVA:
LINE (251, 101)-(349, 199), 2, BF
SOUND 200, 2
RETURN

TRE:
LINE (401, 101)-(499, 199), 1, BF
SOUND 300, 2
RETURN


DIE:
CLS
IF KLAR = 1 THEN
  PRINT "DU KLARADE"; KLAR; "OMGèNG"
  ELSE PRINT "DU KLARADE"; KLAR; "OMGèNGAR"
END IF
PRINT
PRINT "TRYCK ENTER FôR ATT HôRA DIN MELODI..."
DO
LOOP UNTIL INKEY$ = CHR$(13)
MELODI:

FOR I = 1 TO TIMES
IF ALT(I) = 1 THEN SOUND 100, 2
IF ALT(I) = 2 THEN SOUND 200, 2
IF ALT(I) = 3 THEN SOUND 300, 2
FOR P = 1 TO 1500: NEXT P
NEXT I
PRINT "VILL DU HôRA MELODIN IGEN [J/N] ?"
DO
A$ = INKEY$
LOOP UNTIL A$ = "J" OR A$ = "N" OR A$ = "j" OR A$ = "n"
IF A$ = "J" OR A$ = "j" THEN GOTO MELODI
END

SUB CENTER (ROW, TEXT$)

LOCATE ROW, 39 - LEN(TEXT$) / 2
PRINT TEXT$;

END SUB

SUB DRWSCR

LINE (101, 101)-(199, 201), 0, BF
LINE (251, 101)-(349, 201), 0, BF
LINE (401, 101)-(499, 201), 0, BF

LINE (100, 100)-(200, 200), 15, B
LINE (250, 100)-(350, 200), 15, B
LINE (400, 100)-(500, 200), 15, B

LOCATE 16, 19
PRINT "1"

LOCATE 16, 38
PRINT "2"

LOCATE 16, 57
PRINT "3"


END SUB

